using UnityEngine;

public class seleccion_al1 : MonoBehaviour
{
    public AudioSource movea;
    // Start is called once before the first execution of Update after the MonoBehaviour is created
    void Start()
    {
        
    }

    // Update is called once per frame
    void Update()
    {
        
    }
    public void move()
    {
        movea.Play();
    }
}
